# https://www.playframework.com/documentation/latest/Configuration

play.http.context = "/"
play.http.session.maxAge = 30 minutes

play.http.session {
  # The cookie name
  cookieName = "PLAY_SESSION"

  # Whether the secure attribute of the cookie should be set to true
  # In dev it's usually false; set to true behind HTTPS in prod
  secure = false

  # The max age to set on the cookie.
  # If null, the cookie expires when the user closes their browser.
  # An important thing to note, this only sets when the browser will discard the cookie.
  maxAge = null

  # Whether the HTTP only attribute of the cookie should be set to true
  httpOnly = true

  # The value of the SameSite attribute of the cookie. Set to null for no SameSite attribute.
  # Possible values are "lax", "strict" and "none". If misconfigured it's set to null.
  sameSite = "lax"

  # The domain to set on the session cookie
  # If null, does not set a domain on the session cookie.
  domain = null

  # The session path. Must start with /
  path = ${play.http.context}

  jwt {
    # The JWT signature algorithm to use on the session cookie
    # uses 'alg' https://tools.ietf.org/html/rfc7515#section-4.1.1
    signatureAlgorithm = "HS256"

    # The time after which the session is automatically invalidated.
    # Use 'exp' https://tools.ietf.org/html/rfc7519#section-4.1.4
    expiresAfter = ${play.http.session.maxAge}

    # The amount of clock skew to accept between servers when performing date checks
    # If you have NTP or roughtime synchronizing between servers, you can enhance
    # security by tightening this value.
    clockSkew = 5 minutes

    # The claim key under which all user data is stored in the JWT.
    dataClaim = "data"
  }
}

db {
  # Default MySQL datasource
  default.driver = "com.mysql.cj.jdbc.Driver"
  default.url = "jdbc:mysql://127.0.0.1:3306/scala?useSSL=false"
  default.username = "root"
  default.password = ""
  default.migration.initOnMigrate=false
  default.migration.cleanDisabled=false

  default.hikari {
    connectionTimeout = 30 seconds
    maximumPoolSize = 10
  }
}
play.http.secret.key="M1C10taMSz9oGizOWFOiE<L9r49o[Mw4KJN=0Z;VMhl_BXc`_GYV1IZ13qZevjOL"
